interface:
  display_name: "Saturation"
  short_description: "Deliver polished, launch-ready implementations"
  default_prompt: "Use $saturation as a quality-first technical consultancy: perform technical discovery, freeze .saturation/context.md, delegate to the fixed specialist team with deep scoped briefs, iterate through the task-specific harness and launch gates, and return an evidence-backed production candidate."
